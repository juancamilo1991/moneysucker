export interface Product {
    _id: number
    description: string
    price: number
    type: string
    name: string
   

    // getImage(id){
    //     // get the img from service with id
    // }
}